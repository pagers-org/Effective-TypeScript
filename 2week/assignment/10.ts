// 10. 타입스크립트에서 promise보다 async/await를 사용하는게 더 나은 이유를 서술해보세요.
/**
 * 1. async 함수를 사용하면 항상 비동기 코드를 작성하는 것과 같다. 간혹 콜백 지옥에서 의도하지 않은 동기가 발생하는 상황이 있는데, 해당 상황을 미리 방지하기에도 좋다
 * 2. 가독성이 좋아진다. callback 지옥 코드와 await으로 잘 정리된 코드는 가독성의 차이가 크다.
 * 3. async 함수에서 promise를 리턴할때에 리턴 타입은 Promise<T>이다 프로미스로 래핑되지 않아 편리하다.
 */