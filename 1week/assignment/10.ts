/**
 * never, unknown, any 타입에 대해 정리하여 최대한 상세하게 적어주세요.
 *
 * 1. never: 할당될 수 없는 타입으로 any 타입의 값을 포함하여 어떤 값도 가질 수 없다. 따라서 바닥 타입으로도 부른다.
 * 불가능을 나타내기 위해 사용되므로 빈 합집합이나 호환되지 않는 타입의 교차 타입, 허용되지 않는 제너릭의 매개변수 타입으로 쓰인다.
 *
 * 2. unknown: 할당되지 않은 타입으로 아직 무슨 타입인지 모른다. any와 동일하게 모든 값을 허용하나 연산 혹은 객체의 프로퍼티를 확인할 수 없다.
 *
 * 3. any: 어떤 타입도 포용할 수 있는 타입으로 "제한하지 않는"다. 점진적으로 타입을 적용할 때 사용한다.
 */
