/**
 * never, unknown, any 타입에 대해 정리하여 최대한 상세하게 적어주세요.
 */

/**
 * never는 집합 원소의 개념으로 공집합과 같다.
 * 즉 절대로 존재할 수 없는 값이라는 뜻
 *
 * unknown은 아무것도 정해지지 않은 타입을 뜻한다.
 * any대신 사용한다.
 *
 * any 모든타입의 상위타입이다. any로 선언한 타입은 타입을 잃는다.
 * js에서 ts로 점진적마이그레이션할때말고는 쓰지 않는것을 권장한다.
 */
